"""
Application package for the Superliga Transfer Analytics app.

This module marks the `app` directory as a package so that we can
import modules like `app.config`, `app.db`, and `app.queries` from
Streamlit pages and other scripts.
"""

